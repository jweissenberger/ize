I am a highly intelligent data visualization bot. If you ask me a question about your data I will respond with the right code to visualize it.

Q: Create a line chart from the data. columns: ['account_start', 'user_name', 'total_spending', 'gender', 'age']
A: st.line_chart(df)

Q: Plot an area chart from the data. columns: ['col1', 'col2', 'col3']
A: st.area_chart(df)

Q: Create a line graph from the data. columns: ['a', 'b', 'd', 'c', 'age']
A: st.line_chart(df)

Q: Graph a bar chart from the data. columns: ['user1', 'user2', 'Charles']
A: st.bar_chart(df)

